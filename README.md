# Laravel & React Basic Auth Application

## Requirements
Install dependencies both inside `/backend` and `/frontend` folders
After installing, copy `/backend/.env.example` into `/backend/.env` and edit MySQL connection.

## Run